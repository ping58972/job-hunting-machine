[alembic]
script_location = %(here)s/src/job_hunting_machine/database/migrations
prepend_sys_path = %(here)s/src
path_separator = os

# Online migrations use the guarded application engine, never a configurable URL.
# For an isolated local database: uv run --locked alembic -x db=.tmp/example.db upgrade head

# Optional local overrides. Copy to .env; never commit credentials.
# Process JHM_* variables override .env, which overrides YAML configuration.
JHM_RUNTIME_MODE=DRY_RUN
JHM_LOG_LEVEL=INFO
# The project root is fixed by Architecture v2 and cannot be overridden here.
# Phase 3 defaults to mock model transport. Runtime modes alone never enable OpenAI.
# For explicit ModelGateway.for_openai use, supply these in the process environment.
# They are not automatically loaded by the gateway from this file.
OPENAI_ALLOW_LIVE=0
# OPENAI_API_KEY=  # Supply securely; never commit a real value.

# Slack tokens are read only from the process environment, never automatically from .env.
# Live Socket Mode also requires jhm slack --live and populated config/slack.yaml.
SLACK_ALLOW_LIVE=0
# SLACK_BOT_TOKEN=  # Supply securely; never commit a real value.
# SLACK_APP_TOKEN=  # Supply securely; never commit a real value.

# Phase 5 live reads and model use require explicit CLI flags in addition to process flags.
JOB_FETCH_ALLOW_LIVE=0
JOB_BROWSER_ALLOW_LIVE=0
# Keep installed browser binaries under the project root when explicitly installing them.
# PLAYWRIGHT_BROWSERS_PATH=/Users/ping58972/Documents/job-hunting-machine/data/browser-binaries

# Phase 6 GitHub reads also require jhm catalog worker --live.
GITHUB_ALLOW_LIVE=0
# GITHUB_TOKEN=  # Optional least-privilege read token, process environment only.

# Application documents are generated locally with LaTeX. No Google Docs credential is used.
# `jhm resume worker --live-models` still requires the OpenAI gates above.

# Phase 8 real-site form preparation also requires configured LIVE and --live.
# STAGING is limited to the network-free fake ATS.
FORM_BROWSER_ALLOW_LIVE=0

# Phase 9 final submission has a separate opt-in and also requires configured LIVE,
# `jhm submission worker --live`, FORM_BROWSER_ALLOW_LIVE=1, and a current approval.
SUBMISSION_ALLOW_LIVE=0

# Phase 10 public contact reads and Gmail writes are independently gated.
CONTACT_DISCOVERY_ALLOW_LIVE=0
GMAIL_ALLOW_LIVE=0
OUTREACH_SEND_ALLOW_LIVE=0
# GMAIL_ACCESS_TOKEN=  # Short-lived OAuth token with only required Gmail scopes.

# Phase 11 monitoring uses separate read-only Gmail and portal gates. The default
# monitor command contacts neither service. Use a token with gmail.readonly only.
GMAIL_MONITOR_ALLOW_LIVE=0
PORTAL_MONITOR_ALLOW_LIVE=0
# GMAIL_MONITOR_ACCESS_TOKEN=  # Short-lived read-only token; process environment only.

# Phase 12 backup, recovery, reporting, and doctor commands add no LIVE opt-in.
# They never change JHM_RUNTIME_MODE or any provider allow flag.
